logging:
  level:
    net.ttddyy.dsproxy.listener: debug
    root: info
    web: info
spring:
  application:
    name: test
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL:jdbc:postgresql://localhost:5432/mentality_test}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:pass}
  liquibase:
    enabled: false
  sql:
    init:
      mode: always
